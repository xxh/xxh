name: Test xxh on push

on: push

jobs:
  test-short:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'
    - name: Install xxh
      run: pip install .
    - name: Run xxh
      run: xxh --version
